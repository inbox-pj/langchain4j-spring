global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'langchain4j-monitor'

scrape_configs:
  - job_name: 'langchain4j-spring'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']
        labels:
          application: 'langchain4j-spring'
          environment: 'development'

  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

